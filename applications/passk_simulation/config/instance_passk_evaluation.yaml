# Instance-level pass@k evaluation configuration

# Data paths
outputs_dir: ../../outputs
results_dir: ../results/passk_simulation/instance_passk_results
estimator_results_dir: ../../estimator_results

# Optional neurips-style ground truth loading (see instance_passk_evaluation_neurips.yaml):
# ground_truth_source: linear_probe
# ground_truth_linear_probe: neurips
# linear_probe_estimator_results_dir: ../../neurips2026_estimator_results
# linear_probe_configs: {...}
# linear_probe_suffix_by_dataset: {...}
# linear_probe_result_subdir_by_dataset: {...}

# Datasets to evaluate
datasets: ["aime25-test","math-500"]

# K values to evaluate (table columns)
k_values: [1, 4, 16, 64]

# Maximum samples per instance
max_samples: 100

# Metrics to compute for each (method, k) pair
metrics: ["mse"] #"mae", "pearson_r"

# Confidence sources to compare (table rows)
confidence_sources:
  oracle_rc:
    type: "oracle_response"
    label: "Oracle RC"
    
  oracle_cc:
    type: "ground_truth"
    label: "Oracle CC"

  linear_probe_math:
    type: "learned"
    method: "linear_probe"
    probe_suffix: "trained-on-math-train-and-validated-on-math-valid"
    label: "Linear Probe"

# Plotting configuration
plot_config:
  figsize: [10, 6]
  dpi: 150
  bins: 50  # Number of bins for error distribution histogram
